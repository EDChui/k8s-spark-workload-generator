apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: spark-executor-nonpreempting-high
value: 100000
globalDefault: false
preemptionPolicy: Never
description: "Executors schedule before drivers, but must not preempt running pods"
